{
  "buildCommand": "pnpm --filter @workspace/vertex-finance run build",
  "outputDirectory": "artifacts/vertex-finance/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
